<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather's App</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inconsolata&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        font-family: "Inconsolata", monospace;
        margin: 0;
        padding: 0;
      }

      body {
        background-color: #fffadd;
        display: flex;
        justify-content: center;
      }
      .container {
        display: flex;
        flex-direction: column;
        margin-top: 7rem;
        gap: 2.5rem;
        background-color: #8ecddd;
        margin-top: 5rem;
        padding: 2rem 25rem 4rem 25rem;
        border-radius: 15px;
        box-shadow: 10px 10px rgba(128, 128, 128, 0.4);
      }
      .title {
        color: #22668d;
        font-size: xx-large;
        text-shadow: 2px 2px gray;
      }
      .sub-title {
        margin-bottom: 1rem;
        font-size: x-large;
      }

      #city {
        width: 10rem;
        height: 2rem;
        border-radius: 15px;
        padding-left: 1rem;
      }

      .btn-search {
        margin-left: 0.25rem;
        padding: 0.5rem 0.75rem 0.5rem 0.75rem;
        border-radius: 10px;
        background-color: rgba(255, 204, 112, 1);
      }

      .btn-search:hover {
        background-color: rgba(255, 204, 112, 0.7);
        color: white;
        border-color: rgba(0, 0, 0, 0.5);
        cursor: pointer;
      }

      .weather {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        background-color: white;
        padding: 2rem 3rem 2rem 3rem;
        border-radius: 10px;
        box-shadow: 1px 1px rgba(128, 128, 128, 0.4);
      }
      .icon {
        display: flex;
        gap: 5rem;
        margin-bottom: 1rem;
        align-items: center;
      }
      .weather-desc {
        display: flex;
        gap: 5rem;
      }
      .weather > p {
        font-size: x-large;
        font-weight: bolder;
        color: rgba(255, 204, 112, 1);
        text-shadow: 1px 1px gray;
      }
      .desc-title {
        color: #22668d;
        font-weight: 900;
        font-size: large;
      }
      .weather-main {
        font-size: larger;
      }
      .error-msg {
        color: red;
        font-size: x-large;
        margin-top: -1.5rem;
      }
      .temp-icon {
        display: flex;
        gap: 1.5rem;
      }
      .desc-data {
        font-weight: 600;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="title">
        <h1>Weather's App</h1>
      </div>
      <div class="form">
        <p class="sub-title">Check your current weather.</p>
        <form action="/search" method="POST" class="input-form">
          <input type="text" name="city" id="city" placeholder="City name" />
          <input type="submit" value="Search" class="btn-search" />
        </form>
      </div>
      <% if(locals.data) { %>
      <div class="weather">
        <p>
          Current weather: <span style="color: #22668d"> <%= data.name %></span>
        </p>
        <div class="icon">
          <div class="temp-icon">
            <% function getImg() { return
            `https://openweathermap.org/img/wn/${data.weather[0].icon}@2x.png` }
            %> <% let imgURL = getImg() %>
            <img src="<%" ="imgURL" % /> alt="weather" >
            <p style="font-size: 48px; color: #22668d; align-self: center">
              <%= Math.floor(data.main.temp) %>&deg;C
            </p>
          </div>
          <div class="weather-main" style="font-weight: 700">
            <p><%= data.weather[0].main %></p>
            <p>
              Feels like
              <span style="color: #22668d"
                ><%= Math.floor(data.main.feels_like) %>&deg;C</span
              >
            </p>
          </div>
        </div>
        <div class="weather-desc">
          <div class="wind">
            <img src="img/wind.png" alt="windspeed" width="50" />
            <p class="desc-title">Wind</p>
            <p class="desc-data"><%= data.wind.speed %> m/s</p>
          </div>
          <div class="humidity">
            <img src="img/humidity.png" alt="windspeed" width="50" />
            <p class="desc-title">Humidity</p>
            <p class="desc-data"><%= data.main.humidity %>%</p>
          </div>
          <div class="visibility">
            <img src="img/visibility.png" alt="windspeed" width="50" />
            <p class="desc-title">Visibility</p>
            <p class="desc-data"><%= data.visibility/1000 %> km</p>
          </div>
          <div class="pressure">
            <img src="img/pressure.png" alt="windspeed" width="50" />
            <p class="desc-title">Pressure</p>
            <p class="desc-data"><%= data.main.pressure %> hPa</p>
          </div>
        </div>
      </div>

      <% } else { %> <% if(locals.err) { %>
      <p class="error-msg"><%= err.message %></p>
      <% } %> <% } %>
    </div>
  </body>
</html>
